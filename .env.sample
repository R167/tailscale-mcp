# Tailscale MCP Server Configuration
# Copy this file to .env and fill in your actual values

# Required: Your tailnet identifier
TAILSCALE_TAILNET=your-tailnet-here

# Authentication Method 1: API Key (simpler setup)
TAILSCALE_API_KEY=your-api-key-here

# Authentication Method 2: OAuth Client Credentials (more secure, granular permissions)
# Use EITHER API key OR client credentials, not both
# TAILSCALE_CLIENT_ID=your-client-id-here
# TAILSCALE_CLIENT_SECRET=your-client-secret-here

# Optional: HTTP server port (defaults to 8080)
PORT=8080